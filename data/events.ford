contract Events

myAddress = address("0xfe3091F63A0b0b1cf81ff53102434aa287aC5289")

def receive(amount) {

    transfer(myAddress, amount);

    emit("Received", {
        address: myAddress,
        amount: amount,
        message: "Received some ether"
    });
}
